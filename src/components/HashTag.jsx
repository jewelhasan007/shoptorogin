

const HashTag = ({hashtag}) => {
    return (
        <div className="my-4 mx-1 grow text-center">
            <h2 className="bg-slate-200 m-0 p-2 rounded-[10px] text-green-700 ">{hashtag}</h2>
        </div>
    );
};

export default HashTag;
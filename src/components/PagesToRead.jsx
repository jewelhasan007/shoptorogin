import { Helmet } from "react-helmet";

const PagesToRead = () => {
    return (
        <div>
            <Helmet><title>Pages to Read|| Book Vibe</title></Helmet>
            READ
        </div>
    );
};

export default PagesToRead;